hide_disclaimer = "YES"
timeout_seconds = 6
ipv4_bind_addresses = "127.0.0.1,10.0.2.1"
chroot_dir = "/etc/maradns"
recursive_acl = "127.0.0.1,10.0.0.0/8"

# if not want query from root dns from IANN,
# define upstream recursive dns server.
upstream_servers = {}
upstream_servers["."] = "202.106.46.151,202.106.0.20,210.77.16.1"

# authoritative dns server
csv2 = {}
csv2["foo.bar."] = "db.foo.bar"
csv2["foobar.ossxp.com."] = "db.foo.bar"

tcp_convert_acl = "0.0.0.0/0"
tcp_convert_server = "10.0.0.2"

# setup slave dns server IPs
zone_transfer_acl = "127.0.0.1,10.0.0.0/8"

# The maximum number of records to display if a record in the additional section 
# (e.g., the IP of a NS server or the ip of a MX exchange) has more than one value.
# This is similar to max_chain, but applies to records in the "additional" (or AR) section. 
max_ar_chain = 2

maradns_uid = 117
